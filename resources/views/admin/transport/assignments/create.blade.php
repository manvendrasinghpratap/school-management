@extends('backend.layout.default')
@section('content')<div class="container-fluid"><h4>Assign Student to Route</h4>@include('admin.transport._flash')<form method="POST" action="{{route('admin.transport.assignments.store')}}">@csrf @include('admin.transport.assignments.form')<button class="btn btn-primary">Assign Student</button> <a href="{{route('admin.transport.assignments.index')}}" class="btn btn-light">Cancel</a></form></div>@endsection
