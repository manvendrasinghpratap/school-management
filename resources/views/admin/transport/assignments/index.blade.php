@extends('backend.layout.default')
@section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-3"><h4>Student Route Assignments</h4><a href="{{route('admin.transport.assignments.create')}}" class="btn btn-primary">Assign Student</a></div>@include('admin.transport._flash')<form class="row g-2 mb-3"><div class="col-md-5"><input name="search" value="{{request('search')}}" class="form-control" placeholder="Search student or route"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All Statuses</option>@foreach(['active','inactive'] as $s)<option value="{{$s}}" @selected(request('status')===$s)>{{ucfirst($s)}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div></form><div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>Student</th><th>Route</th><th>Pickup</th><th>Dropoff</th><th>Start</th><th>End</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($assignments as $a)<tr><td>{{$a->student?->first_name}} {{$a->student?->middle_name}} {{$a->student?->last_name}}</td><td>{{$a->route?->name}}</td><td>{{$a->pickup_point?:'—'}}</td><td>{{$a->dropoff_point?:'—'}}</td><td>{{$a->start_date?->format('d M Y')?:'—'}}</td><td>{{$a->end_date?->format('d M Y')?:'—'}}</td><td><span class="badge bg-{{$a->status==='active'?'success':'secondary'}}">{{ucfirst($a->status)}}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{route('admin.transport.assignments.edit',$a)}}">Edit</a> <form class="d-inline" method="POST" action="{{route('admin.transport.assignments.destroy',$a)}}" onsubmit="return confirm('Delete this assignment?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="8" class="text-center">No assignments found.</td></tr>@endforelse</tbody></table>{{$assignments->links()}}</div></div></div>@endsection
