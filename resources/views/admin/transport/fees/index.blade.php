@extends('backend.layout.default')
@section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-3"><h4>Transport Fees</h4><a href="{{route('admin.transport.fees.create')}}" class="btn btn-primary">Add Transport Fee</a></div>@include('admin.transport._flash')<form class="row g-2 mb-3"><div class="col-md-5"><input name="search" value="{{request('search')}}" class="form-control" placeholder="Search student"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All Statuses</option>@foreach(['pending','invoiced','paid','waived'] as $s)<option value="{{$s}}" @selected(request('status')===$s)>{{ucfirst($s)}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div></form><div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>Student</th><th>Route</th><th>Month</th><th>Amount</th><th>Status</th><th>Invoice</th><th>Action</th></tr></thead><tbody>@forelse($fees as $f)<tr><td>{{$f->assignment?->student?->first_name}} {{$f->assignment?->student?->last_name}}</td><td>{{$f->assignment?->route?->name}}</td><td>{{$f->fee_month?->format('M Y')}}</td><td>₹{{number_format((float)$f->amount,2)}}</td><td><span class="badge bg-{{$f->status==='paid'?'success':($f->status==='waived'?'secondary':($f->status==='invoiced'?'info':'warning'))}}">{{ucfirst($f->status)}}</span></td><td>{{$f->invoice_id?:'—'}}</td><td><a class="btn btn-sm btn-outline-primary" href="{{route('admin.transport.fees.edit',$f)}}">Edit</a> <form class="d-inline" method="POST" action="{{route('admin.transport.fees.destroy',$f)}}" onsubmit="return confirm('Delete this fee?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No transport fees found.</td></tr>@endforelse</tbody></table>{{$fees->links()}}</div></div></div>@endsection
