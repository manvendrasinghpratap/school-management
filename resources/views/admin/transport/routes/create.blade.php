@extends('backend.layout.default')
@section('content')<div class="container-fluid"><h4>Add Transport Route</h4>@include('admin.transport._flash')<form method="POST" action="{{route('admin.transport.routes.store')}}">@csrf @include('admin.transport.routes.form')<button class="btn btn-primary">Save Route</button> <a href="{{route('admin.transport.routes.index')}}" class="btn btn-light">Cancel</a></form></div>@endsection
