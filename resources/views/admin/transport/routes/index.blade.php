@extends('backend.layout.default')
@section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-3"><h4>Transport Routes</h4><a href="{{route('admin.transport.routes.create')}}" class="btn btn-primary">Add Route</a></div>@include('admin.transport._flash')<form class="row g-2 mb-3"><div class="col-md-5"><input name="search" value="{{request('search')}}" class="form-control" placeholder="Search route, code or location"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All Statuses</option>@foreach(['active','inactive'] as $s)<option value="{{$s}}" @selected(request('status')===$s)>{{ucfirst($s)}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div></form><div class="card"><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>Name</th><th>Code</th><th>Start → End</th><th>Vehicle</th><th>Driver</th><th>Monthly Fee</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($routes as $r)<tr><td>{{$r->name}}</td><td>{{$r->code?:'—'}}</td><td>{{$r->start_point?:'—'}} → {{$r->end_point?:'—'}}</td><td>{{$r->vehicle?->vehicle_number?:'—'}}</td><td>{{$r->driver?->name?:'—'}}</td><td>₹{{number_format((float)$r->monthly_fee,2)}}</td><td><span class="badge bg-{{$r->status==='active'?'success':'secondary'}}">{{ucfirst($r->status)}}</span></td><td><a class="btn btn-sm btn-outline-info" href="{{route('admin.transport.routes.stops.index',$r)}}">Stops</a> <a class="btn btn-sm btn-outline-primary" href="{{route('admin.transport.routes.edit',$r)}}">Edit</a> <form class="d-inline" method="POST" action="{{route('admin.transport.routes.destroy',$r)}}" onsubmit="return confirm('Delete this route?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="8" class="text-center">No routes found.</td></tr>@endforelse</tbody></table>{{$routes->links()}}</div></div></div>@endsection
