@extends('backend.layout.default')
@section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-3"><div><h4>Route Stops</h4><div class="text-muted">{{ $route->name }} @if($route->code) ({{$route->code}}) @endif</div></div><a href="{{route('admin.transport.routes.stops.create',$route)}}" class="btn btn-primary">Add Stop</a></div>@include('admin.transport._flash')<div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>#</th><th>Stop</th><th>Pickup</th><th>Dropoff</th><th>Address</th><th>Monthly Fee</th><th>Action</th></tr></thead><tbody>@forelse($stops as $s)<tr><td>{{$s->sequence_no}}</td><td>{{$s->name}}</td><td>{{$s->pickup_time?:'—'}}</td><td>{{$s->dropoff_time?:'—'}}</td><td>{{$s->address?:'—'}}</td><td>{{$s->monthly_fee!==null?'₹'.number_format((float)$s->monthly_fee,2):'—'}}</td><td><a class="btn btn-sm btn-outline-primary" href="{{route('admin.transport.stops.edit',$s)}}">Edit</a> <form class="d-inline" method="POST" action="{{route('admin.transport.stops.destroy',$s)}}" onsubmit="return confirm('Delete this stop?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No stops found.</td></tr>@endforelse</tbody></table>{{$stops->links()}}</div></div></div>@endsection
