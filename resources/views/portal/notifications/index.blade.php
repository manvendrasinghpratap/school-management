@extends('portal.layouts.app',['title'=>'Notifications'])
@section('content')<div class="d-flex justify-content-between align-items-center mb-3"><h2>Notifications</h2><form method="POST" action="{{ route('portal.notifications.read-all') }}">@csrf<button class="btn btn-sm btn-outline-primary">Mark all as read</button></form></div><div class="list-group">@forelse($items as $item)<div class="list-group-item {{ $item->read_at ? '' : 'list-group-item-warning' }}"><div class="d-flex justify-content-between"><strong>{{ $item->title }}</strong><small>{{ $item->created_at?->format('d M Y H:i') }}</small></div><p class="mb-1">{{ $item->body }}</p>@if(!$item->read_at)<form method="POST" action="{{ route('portal.notifications.read',$item) }}">@csrf<button class="btn btn-sm btn-link px-0">Mark as read</button></form>@endif</div>@empty<div class="alert alert-info">No notifications.</div>@endforelse</div>{{ $items->links() }}@endsection
