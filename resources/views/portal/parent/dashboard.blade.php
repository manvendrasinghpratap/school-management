@extends('portal.layouts.app',['title'=>'Parent Dashboard'])
@section('content')<h2>Parent Dashboard</h2><p class="text-muted">Welcome, {{ $guardian->full_name }}.</p>@include('portal.partials.cards',['cards'=>[['label'=>'Children','value'=>$students->count()],['label'=>'Outstanding Fees','value'=>number_format($outstanding,2)],['label'=>'Unread Notifications','value'=>$unreadNotifications],['label'=>'Portal Status','value'=>'Active']]])<div class="row g-3">@foreach($students as $student)<div class="col-md-6"><div class="card shadow-sm"><div class="card-body"><h5>{{ $student->full_name }}</h5><p class="mb-1">{{ $student->student_number }}</p><a href="{{ route('portal.parent.child',$student->id) }}" class="btn btn-primary btn-sm">View Child</a></div></div></div>@endforeach</div>@endsection
