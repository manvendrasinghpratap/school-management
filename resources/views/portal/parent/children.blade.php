@extends('portal.layouts.app',['title'=>'My Children'])
@section('content')<h2>My Children</h2><div class="row g-3">@foreach($students as $student)<div class="col-lg-6"><div class="card"><div class="card-body"><h5>{{ $student->full_name }}</h5><p>{{ $student->student_number }}</p>@php($enrollment=$student->enrollments->first())<p class="text-muted">{{ $enrollment?->academicYear?->name }} · {{ $enrollment?->class?->name }} · {{ $enrollment?->section?->name }}</p><a href="{{ route('portal.parent.child',$student->id) }}" class="btn btn-primary btn-sm">Dashboard</a><a href="{{ route('portal.parent.attendance',$student->id) }}" class="btn btn-outline-secondary btn-sm">Attendance</a><a href="{{ route('portal.parent.results',$student->id) }}" class="btn btn-outline-secondary btn-sm">Results</a><a href="{{ route('portal.parent.fees',$student->id) }}" class="btn btn-outline-secondary btn-sm">Fees</a></div></div></div>@endforeach</div>@endsection
